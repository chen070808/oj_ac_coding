#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    int t;
    long long a, b;
    cin >> t;
    while (t--)
    {
        cin >> a >> b;
        cout << a + b << '\n';
    }
    return 0;
}